{
  "name": "astro-shiba-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Astro Shiba - Enterprise Token Launchpad on Stellar",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "typecheck": "turbo run type-check",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "clean": "turbo run clean && rm -rf node_modules .turbo"
  },
  "devDependencies": {
    "turbo": "^2.0.12",
    "@turbo/gen": "^2.0.12"
  },
  "packageManager": "pnpm@9.7.1",
  "engines": {
    "node": ">=18.0.0"
  }
}
